<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MobileAgendamento.Views.HomePage">
    <StackLayout HorizontalOptions="Center" VerticalOptions="CenterAndExpand" >
        <Label>Bem vindo</Label>
        <Button Text="Cadastrar Paciente" Command="{Binding RegisterPatient}" />
        <Button Text="Cadastrar tipo de vacina" Command="{Binding RegisterVaccineType}" />
        <Button Text="Cadastrar Vacina" Command="{Binding RegisterVaccine}" />
        <Button Text="Agendar Vacina" Command="{Binding SendCommand}" />
    </StackLayout>

</ContentPage>